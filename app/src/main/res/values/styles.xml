<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:listDivider">@drawable/listdriver</item>
        <item name="android:windowContentTransitions">true</item><!-- 转场动画 -->
    </style>

    <style name="ThemeGalley" parent="AppTheme" >
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:backgroundDimAmount">0</item>
    </style>

    <style name="NullStyle">
        <item name="bgColor">#0492fb</item>
        <item name="textSize">50sp</item>
        <item name="radius">15dp</item>
        <item name="name">@string/null_str</item>
    </style>

</resources>
